// swift-tools-version:5.0
import PackageDescription

let package = Package(
    name: "AirgappedSigning",
    products: [
        .library(
            name: "AirgappedSigning",
            targets: ["AirgappedSigning"]),
        ],
    targets: [
        .target(
            name: "AirgappedSigning",
            dependencies: [])
        ]
)
